Generate advice for this Token Use dataset.

Data scope: {data_scope}

Signals JSON:
{signals_json}

Pricing context:
{pricing_context}

Output schema:
{output_schema}

The data is redacted. Do not ask for raw prompts. If a recommendation depends on prompt semantics, mark confidence lower and suggest sampling manually.
